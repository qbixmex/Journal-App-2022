import { Typography } from "@mui/material";
import { Box } from "@mui/system";

export const ImageGallery = () => {
  return (
    <Box mt={ 2 }>
      <Typography
        variant="h1"
        fontSize='2rem'
        fontWeight='light'
        color='primary'
      >ImageGallery Component</Typography>
    </Box>
  );
};
